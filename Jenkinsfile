@Library('fp-jenkins-libs')

// This references the pipeline job defined above
def librarydeploy = new com.itv.fp.librarydeployment.Deploy()

// This references the 'build' function in the Build.groovy file
librarydeploy.execute()